{
  "name": "setup-deploy-keys",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm ci && esbuild ./main.js --bundle --platform=node --outfile=dist/index.js && esbuild ./cleanup.js --bundle --platform=node --outfile=dist/cleanup.js",
    "bundle": "esbuild ./main.js --bundle --platform=node --outfile=dist/index.js && esbuild ./cleanup.js --bundle --platform=node --outfile=dist/cleanup.js",
    "format": "prettier --write **/*.js",
    "format-check": "prettier --check **/*.js"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm run build && npm run format",
      "pre-push": "npm run format-check"
    }
  },
  "keywords": [],
  "author": "wtw",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.2.7",
    "@actions/github": "^4.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.12.4",
    "husky": "^4.3.8",
    "prettier": "^2.3.0"
  }
}
